{
  "name": "rmtdco",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/make-machine-mirrors.mjs && node scripts/check-build.mjs",
    "preview": "astro preview",
    "check": "node scripts/check-build.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5.0.0"
  }
}
